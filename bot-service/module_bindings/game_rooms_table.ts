// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.2 (commit 4b41cf3d71aa85cd4e75c96cbb54324481ee0b27).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { GameRooms } from "./game_rooms_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `game_rooms`.
 *
 * Obtain a handle from the [`gameRooms`] property on [`RemoteTables`],
 * like `ctx.db.gameRooms`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.gameRooms.on_insert(...)`.
 */
export class GameRoomsTableHandle {
  tableCache: TableCache<GameRooms>;

  constructor(tableCache: TableCache<GameRooms>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<GameRooms> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `roomId` unique index on the table `game_rooms`,
   * which allows point queries on the field of the same name
   * via the [`GameRoomsRoomIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.gameRooms.roomId().find(...)`.
   *
   * Get a handle on the `roomId` unique index on the table `game_rooms`.
   */
  roomId = {
    // Find the subscribed row whose `roomId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): GameRooms | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.roomId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: GameRooms) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: GameRooms) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: GameRooms) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: GameRooms) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: GameRooms, newRow: GameRooms) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: GameRooms, newRow: GameRooms) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
